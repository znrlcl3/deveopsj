spring:
  ai:
    gemini:
      base-url: https://generativelanguage.googleapis.com/v1/models
      model: gemini-3.1-flash-lite

app:
  registration:
    enabled: true
  privacy:
    operator-name: 서비스 운영자
    contact-email: CHANGE_ME
  security:
    oidc:
      enabled: false
    login:
      max-attempts: 5
      block-duration: 10m

server:
  servlet:
    session:
      timeout: 30m
